/**
 * The approach is to copy an existing object rather than creating an new instance from scratch.
 * Because creating a new object is costly
 * When creating object with complex logic than this pattern is frequently used
 */